FROM python:3.11-slim

WORKDIR /app

# Cài đặt các thư viện hệ thống cần thiết
RUN apt-get update && apt-get install -y \
    gcc \
    libffi-dev \
    libssl-dev \
    && apt-get clean

RUN pip install --upgrade pip

# Copy requirements trước để tận dụng cache
COPY ./Billing/requirements.txt /app/requirements.txt

# Cài đặt các dependencies

RUN pip install --no-cache-dir -r /app/requirements.txt

# Copy toàn bộ source code
COPY ./jwt_shared /app/jwt_shared
COPY ./Billing /app/Billing

EXPOSE 8002

# Chạy uvicorn (skip alembic vì tables đã tồn tại)
CMD ["uvicorn", "Billing.controller.main:app", "--host", "0.0.0.0", "--port", "8002", "--reload"]
